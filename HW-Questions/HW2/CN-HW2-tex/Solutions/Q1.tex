\section*{پاسخ:}

\begin{enumerate}
\item صحیح
\item صحیح
\item غلط، زیرا \lr{UDP} هیچ مکانیزمی برای بافر کردن یا مرتب‌سازی دوباره‌ی \lr{datagram}ها ندارد؛ هر بسته به‌صورت مستقل تحویل داده می‌شود و اگر برنامه نیاز به حفظ ترتیب داشته باشد، باید خودش در سطح \lr{application} آن را پیاده‌سازی کند.
\item غلط، زیرا در \lr{TCP}، برقراری کامل اتصال نیازمند سه مرحله (\lr{3-way handshake}) است که در آن هر دو طرف باید \lr{SYN} ارسال کنند و در نهایت \lr{ACK} دریافت شود؛ تنها ارسال \lr{SYN} از طرف کلاینت اتصال را برقرار نمی‌کند.
\item صحیح
\item غلط، زیرا حتی ارسال‌های مجدد غیرضروری (\lr{unneeded duplicates}) نیز بر \lr{throughput} تأثیر منفی دارند؛ این بسته‌ها هرچند ممکن است در سمت گیرنده نادیده گرفته شوند، اما پهنای باند شبکه را اشغال می‌کنند و باعث هدررفت ظرفیت مؤثر لینک و کاهش کارایی کلی انتقال داده می‌شوند.
\item صحیح
\item صحیح
\end{enumerate}
